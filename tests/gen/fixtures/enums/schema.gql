# Schema

schema {
  query: Query
}

# Query

type Query {
  binaryOrNull: Binary
  binary: Binary!
  userType: USER_TYPE!
}

enum Binary {
  ZERO
  ONE
}

enum USER_TYPE {
  REGULAR_USER
  ADMIN_USER
  TEMP__ADMIN_USER
}
