{
  "name": "graphql-to-elm-example-basic2",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "elm": "0.19.0-bugfix2",
    "graphql-to-elm": "0.6.1",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "clean": "rimraf src-generated",
    "prebuild": "npm run clean && node prebuild",
    "build": "elm make src/Main.elm --output=index.html",
    "serve": "node server",
    "start": "npm run build && npm run serve"
  }
}
