{
  "name": "graphql-to-elm-example-basic",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "graphql-to-elm": "file:../.."
  },
  "scripts": {
    "clean": "rimraf src-generated",
    "prebuild": "yarn clean && node prebuild",
    "build": "elm make src/Main.elm --output=index.html",
    "serve": "node server",
    "start": "yarn build && yarn serve"
  }
}
